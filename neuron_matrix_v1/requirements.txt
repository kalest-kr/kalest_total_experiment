# 이 프로젝트의 필수 의존성 (실제 실행에 사용한 버전은 results/*/run_summary.json 의
# environment 항목에 기록되어 있다).
numpy>=1.24
scipy>=1.10
matplotlib>=3.6
pillow>=9.0

# PyTorch / GPU 는 **필수 의존성이 아니다**. 이 프로젝트는 설치하지 않았고 사용하지 않았다.
# 설치 저장소가 차단된 환경에서는 같은 설치를 반복하지 말고 미실행으로 기록할 것.
